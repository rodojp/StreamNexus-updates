TwitchZ 0.1.0 / beta release staging

Update base URL: https://updates.example.com/twitchz
Channel: beta

Upload the following files to your generic update server:
- beta.yml
- TwitchZ Setup 0.1.0.exe
- TwitchZ Setup 0.1.0.exe.blockmap

Notes:
- Keep the file names unchanged.
- Upload the metadata YAML and installer files to the same base URL.
- beta users should point to channel=beta, public users should point to channel=latest.
